using System;
namespace SpectrumTV
{
    /// <summary>
    /// Define the list of assets to use in xaml documents
    /// these name of assets should be exists in each platform
    /// iOS > Assets.xcassets(PDF) / Android > Resource directories (xml)
    /// </summary>
    ///

    public static class AppImages
    {
        public const string LogoNav = "logo_nav";
        public const string IconClose = "icon_close";
        public const string IconSearch = "icon_search";
    }
}
